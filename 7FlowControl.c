#include <stdio.h>

void main()
{
    /*
    int age = 19;
    scanf("%d", &age);
    printf("\nYour age is %d", age);

    if (age >= 18)
    {
        printf("\nYou are an adult");
        printf(" and eligible to drive\n");
    }

    printf("\nProgram ends here... Thank you!!!\n", age);
    */

    // 60 + -A
    // 50 + -B
    // 40 + -C

    /*
        int marks;
        printf("\nEnter your marks : ");
        scanf_s("%d", &marks);

        if (marks >= 60)
        {
            printf("\nYou got A grade\n");
        }
        else if (marks >= 50)
        {
            printf("\nYou got B grade\n");
        }
        else if (marks >= 40)
        {
            printf("\nYou got C grade\n");
        }
        else {
            printf("\nYou are failed\n");
        }
    */

    // age and gender from user
    // age >= 60 and gender = m --> interest rate is 9%
    // age >= 60 and gender = f --> interest rate is 10%
    // age < 60  --> interest rate is 8%

    /*
    int age;
    char gender;

    printf("\nEnter your gender : ");
    scanf("%c", &gender);

    printf("\nEnter your age : ");
    scanf("%d", &age);

    printf("\nGender = %c and age = %d", gender, age);

    if (age >= 60)
    {
        if (gender == 'm')
        {
            printf("\nRate of interest in 9%%");
        }
        else if (gender == 'f')
        {
            printf("\nRate of interest in 10%%");
        }
    }
    else
    {
        printf("\nRate of interest in 8%%");
    }
    */

    // int no = 11;

    // zero
    // non zero

    // int result = no < 10;
    // printf("Result = %d\n", result);

    // if (no == 10)
    // {
    //     printf("No is 10\n");
    // }

    // printf("No is divisible by 2 and 5\n");

    // int a = 0, b = 10, c = 0;

    // if((a && b) || c) {
    //     printf("true");
    // } else {
    //     printf("false");
    // }
}