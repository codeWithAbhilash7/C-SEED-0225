#include <stdio.h>

void main()
{
    int a = 10; // local variables
    // int b = 40;

    if (a > 5)
    {
        int a = 30;
        int b = 20;
        // printf(" %d %d ", a, b);
    }

    // printf(" %d %d ", a, b);
}
